name: ERP deployment branch completed

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The Biloo ERP production branch has already been materialized and validated."
