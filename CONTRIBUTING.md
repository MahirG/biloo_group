# Contributing

Create a feature branch, keep changes focused, and open a pull request. Run formatting, linting, type checking, unit tests, build and Playwright tests before review. Never include secrets or real customer data. User-facing claims must match implemented behavior.
